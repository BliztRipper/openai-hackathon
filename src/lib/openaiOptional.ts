export const optionalOpenAiDisclosure = {
  coreExperience: 'The core experience works with protected server-side configuration.',
  optionalVoice:
    'Live OpenAI voice requires a backend token endpoint; standard API keys must never be placed in browser code.',
  sponsoredConstraint: 'If an API is added, use OpenAI products only.',
};
